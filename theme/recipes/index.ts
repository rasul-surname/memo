import { buttonRecipe } from '@/theme/recipes/button'
import { containerRecipe } from '@/theme/recipes/container'

const recipes = {
	button: buttonRecipe,
	container: containerRecipe,
}

export default recipes
